---
phase: 03
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/lib/scoring/deepseek-prompt.ts
  - src/app/(chat)/assessment/actions.ts
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
  - EVAL-03
  - RESULT-02

must_haves:
  truths:
    - "completeAssessment Server Action runs scoring engine, generates DeepSeek narrative, and persists all fields in one call"
    - "DeepSeek narrative uses tier-specific tone: 温和劝退 for wishing-type, 正面肯定 for 高度适配/中度适配"
    - "Fallback narrative is used when DeepSeek API is unavailable or fails"
    - "completeAssessment is idempotent -- duplicate calls skip re-computation"
    - "DEEPSEEK_API_KEY is validated at runtime inside the Server Action"
    - "User authorization check ensures only assessment owner can complete it"
  artifacts:
    - path: "src/lib/scoring/deepseek-prompt.ts"
      provides: "DeepSeek system prompt builder (4 tier variants) and tier-specific fallback narratives"
      exports: ["buildDeepSeekPrompt", "getFallbackNarrative"]
    - path: "src/app/(chat)/assessment/actions.ts"
      provides: "Extended completeAssessment Server Action with scoring + DeepSeek + persistence"
      exports: ["completeAssessment", "createAssessment", "getInProgressAssessment", "saveChatMessages"]
  key_links:
    - from: "src/app/(chat)/assessment/actions.ts"
      to: "src/lib/scoring/engine.ts"
      via: "import { calculateScore }"
      pattern: "import.*calculateScore"
    - from: "src/app/(chat)/assessment/actions.ts"
      to: "src/lib/scoring/deepseek-prompt.ts"
      via: "import { buildDeepSeekPrompt, getFallbackNarrative }"
      pattern: "import.*buildDeepSeekPrompt"
    - from: "src/app/(chat)/assessment/actions.ts"
      to: "@ai-sdk/deepseek"
      via: "createDeepSeek + generateText"
      pattern: "createDeepSeek"

---

<objective>
Extend completeAssessment Server Action to run scoring engine, generate AI narrative via DeepSeek, and persist all evaluation data in one atomic call.

Purpose: Implement the backend pipeline that bridges Phase 2 (chat) with Phase 3 (result display). When the assessment chat completes, completeAssessment runs:
1. Fetch collected parameters (annualCapital, weeklyTime, expectedReturn, investmentAmount)
2. Run calculateScore() from Plan 01 (detects wishing-type, computes weighted score, assigns tier)
3. Call DeepSeek generateText with tier-specific prompt to produce personalized narrative
4. Persist score, tier, is_wishing_type, ai_narrative to Supabase in one UPDATE
5. Handle DeepSeek failure gracefully (use fallback narrative strings)

Output:
- deepseek-prompt.ts with system/user prompt builders and tier-specific fallback narratives
- Extended completeAssessment Server Action with scoring + DeepSeek + persistence
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-评估引擎/03-CONTEXT.md (D-04 through D-11 -- Decision context for DeepSeek and prompt strategy)
@.planning/phases/03-评估引擎/03-UI-SPEC.md (Copywriting Contract -- exact tone anchors and fallback narratives by tier)
@.planning/phases/03-评估引擎/03-RESEARCH.md (Architecture Pattern 1: Extended completeAssessment, Pitfall 5: Idempotency, Pitfall 1: Latency)

<interfaces>
From src/app/(chat)/assessment/actions.ts (current completeAssessment, lines 63-82):
```typescript
export async function completeAssessment(assessmentId: string): Promise<{ success: boolean } | { error: string }> {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return { error: 'unauthenticated' };
  const { error } = await supabase
    .from('assessments')
    .update({ status: 'completed' })
    .eq('id', assessmentId)
    .eq('user_id', user.id);
  if (error) return { error: error.message };
  return { success: true };
}
```

From src/lib/scoring/engine.ts (Plan 01, created by 03-01):
```typescript
export function calculateScore(input: ScoringInput): ScoringResult {
  // Returns: { score: number, tier: '高度适配'|'中度适配'|'需要准备', isWishingType: boolean, subScores: SubScores }
}
```

From src/types/assessment.ts (Plan 01):
```typescript
export type Tier = '高度适配' | '中度适配' | '需要准备';
export interface ScoringInput { annualCapital, weeklyTime, expectedReturn, investmentAmount: number | null }
export interface ScoringResult { score: number, tier: Tier, isWishingType: boolean, subScores: SubScores }
```

From src/components/chat/AssessmentChat.tsx (caller -- line ~147):
```typescript
// useEffect on state.isComplete calls completeAssessment(assessmentId), then saves messages, then router.push('/result')
```

UI-SPEC.md Narrative Tone Anchors (D-07):
- 许愿型: 温和劝退, acknowledge enthusiasm, identify mismatches, encourage prep
- 高度适配: 正面肯定 + action-oriented
- 中度适配: 正面肯定 + specific advice
- 需要准备 (non-wishing): honest but constructive

UI-SPEC.md Fallback narratives (when DeepSeek unavailable):
- 高度适配: "您的各项创业条件表现出色..."
- 中度适配: "您已具备良好的创业基础..."
- 需要准备 (non-wishing): "创业是一段需要充分准备的旅程..."
- 需要准备 (wishing-type): "创业热情是非常宝贵的资源..."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DeepSeek prompt builder with tier-specific prompts and fallback narratives</name>
  <files>
    src/lib/scoring/deepseek-prompt.ts
  </files>

  <read_first>
    .planning/phases/03-评估引擎/03-UI-SPEC.md (read the Copywriting Contract section -- exact tone anchors and fallback narratives per tier)
    .planning/phases/03-评估引擎/03-CONTEXT.md (read D-07 and D-08 for tone strategy)
    src/types/assessment.ts (read the Tier type exported from Plan 01)
  </read_first>

  <action>
    CREATE src/lib/scoring/deepseek-prompt.ts with these exact implementations:

    ```typescript
    import type { Tier } from '@/types/assessment';

    export interface DeepSeekPromptInput {
      annualCapital: number | null;
      weeklyTime: number | null;
      expectedReturn: number | null;
      investmentAmount: number | null;
    }

    /**
     * Builds system and user prompts for DeepSeek narrative generation.
     * D-07: Tone varies by tier and wishing-type status.
     * D-10: Non-streaming text generation (generateText, not streamText).
     */
    export function buildDeepSeekPrompt(
      params: DeepSeekPromptInput,
      tier: Tier,
      isWishingType: boolean
    ): { system: string; user: string } {
      const annualCapital = params.annualCapital ?? 0;
      const weeklyTime = params.weeklyTime ?? 0;
      const expectedReturn = params.expectedReturn ?? 0;
      const investmentAmount = params.investmentAmount ?? 0;

      // D-07: System prompt varies by tier/wishing-type
      let system: string;

      if (isWishingType) {
        system = '你是一位经验丰富的创业顾问，需要用温和、鼓励但不失诚实的方式向一位创业意愿强烈的用户提供评估反馈。用户预期回报率过高，需要引导其调整预期。请使用友善的措辞，肯定对方的热情，同时指出当前预期与现实之间的差距。不要使用评判性语言，以"目前阶段"、"建议先"等温和措辞为主。';
      } else if (tier === '高度适配') {
        system = '你是一位经验丰富的创业顾问，需要向一位各项条件优秀的用户提供评估反馈。请使用正面肯定的语言，肯定用户的条件配置，引导用户尽快与专业顾问深入交流。语气要积极、专业、有行动导向。';
      } else if (tier === '中度适配') {
        system = '你是一位经验丰富的创业顾问，需要向一位基础良好的用户提供评估反馈。请在肯定的同时给出有针对性的改进建议，引导用户通过顾问咨询来优化薄弱环节。语气要积极、建设性。';
      } else {
        // 需要准备 (non-wishing)
        system = '你是一位经验丰富的创业顾问，需要向一位条件尚不充分的用户提供评估反馈。请诚实但建设性地指出需要准备的方面，避免打击积极性。语气要温和、鼓励，强调"这是更聪明的准备"而不是"你不行"。';
      }

      const user = `请根据以下用户的创业评估数据，生成一段3-5句话的个性化评估叙述段落：

    【用户数据】
    - 年度弹性资金：${annualCapital}万元
    - 每周可投入时间：${weeklyTime}小时
    - 预期年化回报率：${expectedReturn}%
    - 投入金额：${investmentAmount}万元

    要求：
    1. 引用至少一项用户的具体数据（如"您每年有${annualCapital}万元的弹性资金"），使叙述个性化
    2. 不要直接引用综合评分数字（评分已在页面上方展示）
    3. 语言自然流畅，不分点列举
    4. 控制在3-5句话`;

      return { system, user };
    }

    /**
     * Returns hardcoded fallback narrative paragraph for each tier.
     * Used when DeepSeek API is unavailable, times out, or returns an error.
     * D-07: Each variant follows the prescribed tone strategy.
     */
    export function getFallbackNarrative(tier: Tier, isWishingType: boolean): string {
      if (isWishingType) {
        // 温和劝退: acknowledge enthusiasm, encourage preparation
        return '创业热情是非常宝贵的资源，但合理的预期同样重要。目前建议您先深入了解所在行业的实际回报数据，调整预期后再做决策，这样创业成功的概率会大大提高。';
      }
      if (tier === '高度适配') {
        // 正面肯定 + 行动引导
        return '您的各项创业条件表现出色，资金储备、时间投入和回报预期都在合理范围内。建议您尽快与我们的专属顾问沟通，制定具体的落地方案。';
      }
      if (tier === '中度适配') {
        // 正面肯定 + 具体建议
        return '您已具备良好的创业基础，部分条件还有进一步优化的空间。我们的顾问可以根据您的具体情况，帮助您找到最适合的创业路径。';
      }
      // 需要准备 (non-wishing): 诚实但建设性
      return '创业是一段需要充分准备的旅程。目前来看，还有一些基础条件可以进一步夯实。建议先与顾问做一次初步沟通，了解您需要为哪些方面做准备。';
    }
    ```
  </action>

  <verify>
    <automated>
      test -f src/lib/scoring/deepseek-prompt.ts &&
      grep -q "export function buildDeepSeekPrompt" src/lib/scoring/deepseek-prompt.ts &&
      grep -q "export function getFallbackNarrative" src/lib/scoring/deepseek-prompt.ts &&
      grep -q "创业热情是非常宝贵的资源" src/lib/scoring/deepseek-prompt.ts &&
      grep -q "您的各项创业条件表现出色" src/lib/scoring/deepseek-prompt.ts &&
      npx tsc --noEmit --pretty false 2>&1 | grep -c "error TS" | grep -q "^0$"
    </automated>
  </verify>

  <acceptance_criteria>
    - src/lib/scoring/deepseek-prompt.ts exists and exports buildDeepSeekPrompt and getFallbackNarrative
    - buildDeepSeekPrompt returns object with `system` and `user` string properties
    - getFallbackNarrative returns 4 distinct strings depending on tier/isWishingType
    - Fallback narratives match the UI-SPEC.md copywriting contract verbatim
    - `npx tsc --noEmit --pretty false` has 0 errors
  </acceptance_criteria>

  <done>DeepSeek prompt builder created with 4-tier system prompt variants and 4 fallback narratives. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extend completeAssessment Server Action with scoring, DeepSeek narrative generation, and idempotency guard</name>
  <files>
    src/app/(chat)/assessment/actions.ts
  </files>

  <read_first>
    src/app/(chat)/assessment/actions.ts (read the ENTIRE current file -- all exported functions, all existing patterns)
    src/lib/scoring/engine.ts (read calculateScore interface)
    src/lib/scoring/deepseek-prompt.ts (read buildDeepSeekPrompt, getFallbackNarrative interfaces)
    .planning/phases/03-评估引擎/03-RESEARCH.md (read Pitfall 5 about idempotency guard, Pattern 1 about architecture flow)
  </read_first>

  <action>
    There are TWO changes to src/app/(chat)/assessment/actions.ts:

    ### 2a. Add imports at the top of the file (after the existing `import { createClient }` line):

    ```typescript
    import { calculateScore } from '@/lib/scoring/engine';
    import { buildDeepSeekPrompt, getFallbackNarrative } from '@/lib/scoring/deepseek-prompt';
    ```

    ### 2b. REPLACE the entire existing completeAssessment function (lines 63-82) with:

    ```typescript
    /**
     * Extended Server Action: runs scoring engine (EVAL-01), detects wishing-type (EVAL-02),
     * generates DeepSeek narrative (RESULT-02), persists all fields (EVAL-03).
     *
     * D-01: Rule-based scoring (not AI-generated).
     * D-05: Wishing-type forced tier is handled inside calculateScore.
     * D-09: DeepSeek is called server-side for narrative text only (not scoring).
     * D-10: Non-streaming generateText, full response persisted to DB.
     */
    export async function completeAssessment(assessmentId: string): Promise<{ success: boolean } | { error: string }> {
      const supabase = await createClient();

      const { data: { user } } = await supabase.auth.getUser();
      if (!user) {
        return { error: 'unauthenticated' };
      }

      // Pitfall 5 guard: Idempotency -- skip if score already computed
      // Prevents duplicate DeepSeek API calls on React Strict Mode re-render
      const { data: check } = await supabase
        .from('assessments')
        .select('score')
        .eq('id', assessmentId)
        .eq('user_id', user.id)
        .single();

      if (!check) {
        return { error: 'assessment not found' };
      }

      if (check.score !== null) {
        // Already completed -- skip re-computation
        return { success: true };
      }

      // 1. Fetch collected parameters
      const { data: assessment, error: fetchError } = await supabase
        .from('assessments')
        .select('annual_capital, weekly_time, expected_return, investment_amount')
        .eq('id', assessmentId)
        .eq('user_id', user.id)
        .single();

      if (fetchError || !assessment) {
        return { error: 'assessment not found' };
      }

      // 2. Run scoring engine (V4 Access Control: user_id scoped above)
      const scoring = calculateScore({
        annualCapital: assessment.annual_capital,
        weeklyTime: assessment.weekly_time,
        expectedReturn: assessment.expected_return,
        investmentAmount: assessment.investment_amount,
      });

      // 3. Generate narrative via DeepSeek (server-side, non-streaming)
      // If DeepSeek fails (network, timeout, missing key), use fallback narrative string
      let aiNarrative: string;
      try {
        if (!process.env.DEEPSEEK_API_KEY) {
          throw new Error('DEEPSEEK_API_KEY not configured');
        }

        // Dynamic import avoids loading the SDK module when key is missing
        const { createDeepSeek } = await import('@ai-sdk/deepseek');
        const { generateText } = await import('ai');

        const deepseek = createDeepSeek({ apiKey: process.env.DEEPSEEK_API_KEY });

        // IMPORTANT: Map snake_case DB fields to camelCase DeepSeekPromptInput
        // (same pattern as calculateScore call above -- prevents all-zero narrative bug)
        const { system, user } = buildDeepSeekPrompt(
          {
            annualCapital: assessment.annual_capital,
            weeklyTime: assessment.weekly_time,
            expectedReturn: assessment.expected_return,
            investmentAmount: assessment.investment_amount,
          },
          scoring.tier,
          scoring.isWishingType
        );

        const { text } = await generateText({
          model: deepseek('deepseek-v4-flash'),
          messages: [
            { role: 'system', content: system },
            { role: 'user', content: user },
          ],
        });

        aiNarrative = text;
      } catch {
        // Fallback: use hardcoded tier-specific narrative
        aiNarrative = getFallbackNarrative(scoring.tier, scoring.isWishingType);
      }

      // 4. Persist all scoring fields + status in one atomic UPDATE
      const { error: updateError } = await supabase
        .from('assessments')
        .update({
          status: 'completed',
          score: scoring.score,
          tier: scoring.tier,
          is_wishing_type: scoring.isWishingType,
          ai_narrative: aiNarrative,
        })
        .eq('id', assessmentId)
        .eq('user_id', user.id);

      if (updateError) {
        return { error: updateError.message };
      }

      return { success: true };
    }
    ```

    Keep ALL other exported functions (createAssessment, getInProgressAssessment, saveChatMessages) completely unchanged. Do not modify their implementation.
  </action>

  <verify>
    <automated>
      grep -q "import { calculateScore }" src/app/\(chat\)/assessment/actions.ts &&
      grep -q "import { buildDeepSeekPrompt" src/app/\(chat\)/assessment/actions.ts &&
      grep -q "check.score !== null" src/app/\(chat\)/assessment/actions.ts &&
      grep -q "createDeepSeek" src/app/\(chat\)/assessment/actions.ts &&
      grep -q "ai_narrative: aiNarrative" src/app/\(chat\)/assessment/actions.ts &&
      grep -q "annualCapital: assessment.annual_capital" src/app/\(chat\)/assessment/actions.ts &&
      grep -q "getFallbackNarrative" src/app/\(chat\)/assessment/actions.ts &&
      npx tsc --noEmit --pretty false 2>&1 | grep -c "error TS" | grep -q "^0$"
    </automated>
  </verify>

  <acceptance_criteria>
    - completeAssessment imports calculateScore, buildDeepSeekPrompt, getFallbackNarrative
    - completeAssessment has idempotency guard: checks existing score before re-computation
    - completeAssessment uses dynamic import for @ai-sdk/deepseek and ai (avoids loading SDK when key is missing)
    - completeAssessment uses getFallbackNarrative in the catch block
    - completeAssessment maps snake_case DB fields to camelCase when calling buildDeepSeekPrompt (fixes all-zero narrative bug)
    - completeAssessment UPDATEs score, tier, is_wishing_type, ai_narrative, status
    - All other 3 exported functions (createAssessment, getInProgressAssessment, saveChatMessages) are unchanged
    - 0 TypeScript errors in npx tsc --noEmit
  </acceptance_criteria>

  <done>Extended completeAssessment implements scoring + DeepSeek narrative + persistence with idempotency guard, snake_case-to-camelCase mapping, and fallback handling. All existing exports preserved. TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser <-> Server Action | Client calls completeAssessment(assessmentId); all scoring/narrative computation happens server-side |
| Server Action -> DeepSeek API | DEEPSEEK_API_KEY crosses from server env to DeepSeek's external API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | DEEPSEEK_API_KEY exposure in client bundle | mitigate | createDeepSeek is called only inside the Server Action via dynamic import. DEEPSEEK_API_KEY is NOT prefixed with NEXT_PUBLIC_ (confirmed: bare name). Dynamic import ensures the @ai-sdk/deepseek module is never bundled client-side. |
| T-03-02-02 | Information Disclosure | User reads another user's assessment | mitigate | All assessments queries use `.eq('user_id', user.id)` scoping. Supabase RLS (migration 002) enforces `auth.uid() = user_id` on all operations -- defense in depth. |
| T-03-02-03 | Tampering | Score manipulation via client | mitigate | Scoring is pure server-side (calculateScore in engine.ts). Client only sends assessmentId string. No scoring input values can be manipulated by the client. |
| T-03-02-04 | Tampering | DeepSeek prompt injection | mitigate | All prompt-injected values are numeric (annualCapital, weeklyTime, expectedReturn, investmentAmount). No user free-text enters the prompt. Low injection risk. |
| T-03-02-05 | Denial of Service | Duplicate DeepSeek API calls | mitigate | Idempotency guard checks `check.score !== null` before proceeding. Subsequent calls return `{ success: true }` immediately -- no DeepSeek API charge for duplicates. |
| T-03-02-06 | Information Disclosure | DeepSeek error messages leaked to client | accept | Errors are caught in the try/catch block and replaced with fallback narrative. Client only sees `{ success: true }` or `{ error: string }` -- never raw API error details. |
</threat_model>

<verification>
1. `npx tsc --noEmit --pretty false 2>&1 | grep -c "error TS"` -- returns 0
2. `grep -c "calculateScore" src/app/\(chat\)/assessment/actions.ts` -- returns at least 1
3. `grep -c "getFallbackNarrative" src/app/\(chat\)/assessment/actions.ts` -- returns at least 1
4. `grep -c "NEXT_PUBLIC_DEEPSEEK_API_KEY" src/` -- returns 0 (env var NOT client-exposed)
5. `grep -c "createAssessment" src/app/\(chat\)/assessment/actions.ts` -- returns 1 (preserved)
6. `grep -c "getInProgressAssessment" src/app/\(chat\)/assessment/actions.ts` -- returns 1 (preserved)
7. `grep -c "saveChatMessages" src/app/\(chat\)/assessment/actions.ts` -- returns 1 (preserved)
</verification>

<success_criteria>
- Extended completeAssessment runs scoring + DeepSeek narrative + DB persistence atomically
- Idempotency guard prevents duplicate DeepSeek calls on React Strict Mode re-render
- Fallback narrative used when DeepSeek API key is missing or call fails
- Snake_case DB fields correctly mapped to camelCase before passing to buildDeepSeekPrompt
- All four scoring fields (score, tier, is_wishing_type, ai_narrative) persisted in one atomic UPDATE
- 0 TypeScript errors after compilation
- All 3 existing exports unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-评估引擎/03-02-SUMMARY.md`
</output>
